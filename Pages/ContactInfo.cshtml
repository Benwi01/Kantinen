@page
@model Ibas.MenuApp.Pages.ContactInfoModel
@{
}

<style>
    /* General Body Styling */
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #fafafa;
        color: #333;
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 20px;
        margin: 0;
    }

    /* Title Styling */
    h1 {
        color: #f26b38; /* Warm orange */
        font-size: 3em;
        margin-bottom: 20px;
    }

    h2 {
        color: #1f8a70; /* Teal */
        margin-top: 10px;
        font-size: 1.5em;
    }

    /* Table Styling */
    table {
        width: 80%;
        max-width: 800px;
        margin: 30px auto;
        border-collapse: collapse;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.1);
        background-color: #ffffff;
    }

    th, td {
        padding: 20px;
        text-align: left;
        font-size: 1.1em;
        border: 1px solid #ddd;
    }

    th {
        background-color: #f26b38; /* Warm orange */
        color: white;
        font-weight: bold;
        text-transform: uppercase;
    }

    /* Row Styling */
    tr:nth-child(even) {
        background-color: #e7f1f1; /* Light teal */
    }

    tr:nth-child(odd) {
        background-color: #ffffff;
    }

    /* Footer Styling */
    .footer {
        margin-top: 40px;
        font-size: 0.9em;
        color: #6c757d;
    }
</style>

<h1>Kontaktinformation</h1>

<table>
    <tr>
        <th>Navn</th>
        <td>Karen</td>
    </tr>
    <tr>
        <th>E-mail</th>
        <td>Karen777@mail.dk</td>
    </tr>
    <tr>
        <th>Telefonnummer</th>
        <td>30607239</td>
    </tr>
</table>

<!-- Footer (if applicable) -->
<div class="footer">© Kantinens menu, Uge 45</div>


